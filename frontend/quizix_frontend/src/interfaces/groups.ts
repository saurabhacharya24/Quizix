export interface IGroups {
    group_desc: string
    group_id: string
    group_name: string
    is_admin: boolean
    num_of_members: number
}

export interface ISearchGroups {
    group_name: string
    group_desc: string
    group_id: number
}